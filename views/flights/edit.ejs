<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/flights/forms.css" />
<%- include('../partials/nav') %>

<main>
  <h1><%= title %></h1>
  <form id="new-form" action="/flights/<%= flight._id %>?_method=PUT" method="POST">

    <label for="airline-select">Airline:</label>
    <select name="airline">

      <option value=" <%= flight.airline %>"> <%= flight.airline %></option>
      <option value="American">American</option>
      <option value="Southwest">Southwest</option>
      <option value="United">United</option>
    </select>
    <label for="airport-select">Airport:</label>
    <select name="airport">

      <option value="<%= flight.airport%>"> <%= flight.airport %></option>
      <option value="AUS">AUS</option>
      <option value="DFW">DFW</option>
      <option value="DEN">DEN</option>
      <option value="LAX">LAX</option>
      <option value="SAN">SAN</option>
    </select>
    <label for="flightNo-input">Flight Number:</label>
    <input type="text" name="flightNo" id="flightNo-input" value=" <%= flight.flightNo %>">
    <label for="departs-input">Departure:</label>
    <input type="datetime-local" name="departs" id="datetime-local" value="<%= flight.departs.toISOString().slice(0, 16) %>">

    <button type="submit">Save changes to flight</button>
  </form>
</main>

<%- include("../partials/footer") %>